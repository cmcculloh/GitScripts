# Copy this file and rename it environment_config.overrides, then uncomment the following and set it to the settings your environment requires
# programs_root=/c/Program\ Files\ \(x86\)/
# git_dir="${programs_root}Git/"
# git_install="${git_dir}"etc/
# GIT_HOME="${git_dir}bin/"
# GIT_EXEC="${GIT_HOME}git.exe"






chrome="${HOME}\AppData\Local\Google\Chrome\Application\chrome.exe"
default_browser="${chrome}"
